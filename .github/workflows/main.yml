name: Build Android APK
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Build with Buildozer
        uses: inclem/buildozer-action@v1.2.0
        with:
          workdir: .
          buildozer_version: stable
